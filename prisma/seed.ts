async function main() {
  console.log('Seed bloqueado: este proyecto no carga datos ficticios.');
  console.log('Carga cuarteles, moviles, recursos e incidentes reales desde la app o APIs.');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
